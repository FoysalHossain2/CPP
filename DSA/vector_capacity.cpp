#include<bits/stdc++.h>
using namespace std;

int main()
{

    vector<int> v;
    cout << v.capacity() << endl; // capacity of vector
    v.push_back(10);
    cout << v.capacity() << endl; // capacity of vector
    v.push_back(10);
    cout << v.capacity() << endl; // capacity of vector
    v.push_back(10);
    cout << v.capacity() << endl; // capacity of vector
    v.push_back(10);
    cout << v.capacity() << endl; // capacity of vector
    v.push_back(10);
    cout << v.capacity() << endl; // capacity of vector

    return 0;
}